package twentyone

/**
  * This package contains all class and method for a functional game.
  * This can be used for a commandline tool of for a web application.
  * For now this is included inside the play app,
  * for easier use this could be separated into a other repository, git and maven (like sonatype).
  */
package object utils {
  // Docs only
}